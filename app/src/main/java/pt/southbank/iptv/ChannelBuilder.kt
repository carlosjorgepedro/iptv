package pt.southbank.iptv

import pt.southbank.library.models.Channel

class ChannelBuilder {
    fun <T> buldChannel(lines: T): Channel where T : Iterable<String> {
        TODO("All is dead!")
    }
}